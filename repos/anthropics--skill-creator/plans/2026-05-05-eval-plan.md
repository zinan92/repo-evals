# anthropics/skill-creator eval plan — 2026-05-05

> Archetype: **hybrid-skill** (heavy LLM + 9 Python scripts + 3 LLM
> sub-agents + HTML eval viewer). Layer: **molecule** (multiple
> distinct user-invocable capabilities: create / validate / improve
> description / package / run-eval / iterative-improve / benchmark /
> generate-report).

## Validate now (static)

| Claim | Method |
|---|---|
| 001 SKILL.md ≥ 400 lines + frontmatter | wc -l + head |
| 002 9 scripts non-trivial | wc -l on each .py |
| 003 3 sub-agent prompts ≥ 5 KB | wc -c each agent .md |
| 004 eval-viewer real | wc -l on generate_review.py + viewer.html |
| 005 schemas.md ≥ 200 lines | wc -l |
| 006 LICENSE in subdir | curl LICENSE.txt |

## Cannot validate now (claim-007)

End-to-end skill creation: needs Claude Code + API key + multi-round
iterative improvement loop.

## What counts as passing

- 001..006: each artefact present + non-trivial
- 007 (deferred): real Claude Code session creates a skill, runs
  one iteration of run_loop, generates viewer.html

## Evidence

`runs/2026-05-05/run-static-checks/`:

- `artifacts/{SKILL.md, LICENSE.txt, schemas.md, agent-*, script-*,
  eval-generate-review.py, eval-viewer.html}`
- `logs/static-checks.txt`
- `run-summary.yaml`, `business-notes.md`
