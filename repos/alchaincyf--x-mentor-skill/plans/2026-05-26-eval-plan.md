# Eval Plan — alchaincyf/x-mentor-skill (prompt-skill, atom)

> Ceiling reminder. A prompt-skill repo whose core LLM-driven layer is untested
> caps at `usable` / `available`. This eval is static-only — claim-004 is
> explicitly `skip` with skip_reason recorded.

## What This Repo Claims

- **claim-001 (critical)**: SKILL.md has valid YAML frontmatter so agent hosts load it without falling back to chat.
- **claim-002 (critical)**: Every relative reference SKILL.md mentions (`./references/*.md`, `./references/research/*.md`) actually exists.
- **claim-003 (critical)**: Each capability the README advertises (write tweet / pick topic / review / growth / account diagnosis) maps to a real step-by-step scenario inside SKILL.md.
- **claim-004 (critical, skipped)**: A real LLM run produces output following SKILL.md's routing + format + algorithm contract. Skipped in this eval — see skip_reason.
- **claim-005 (high)**: SKILL.md spells out concrete style / format rules (1/3/1 cadence, hook formula tags, external-link policy, confidence tagging) — not vague wishes.
- **claim-006 (high)**: Methodology in references/research/*.md cites primary sources (Ship 30 articles, X's open-source algorithm code), not Zhihu / WeChat aggregators.
- **claim-007 (high)**: Repo is alive (MIT, recently pushed) and authored by a real, verifiable maintainer with other public work.

## What We Will Validate

### Static layer (this run covers all of this)

- SKILL.md frontmatter parses (claim-001)
- 11 relative references resolve to real files with line counts matching the index (claim-002)
- README capability table mapped to SKILL.md line ranges (claim-003)
- SKILL.md style-rule lines quoted with line numbers (claim-005)
- Research file citation quality spot-checked (claim-006)
- `gh api` snapshot of license / pushed_at / archived state (claim-007)

### Live layer (NOT done this run)

- claim-004 deferred. Would require installing the skill into Claude Code, invoking a real prompt across scenarios A–E, capturing transcripts, and scoring against the SKILL.md contract.

## Real Inputs We Will Use

Static — file reads + GitHub API only:
- `/tmp/x-mentor-skill/SKILL.md`
- `/tmp/x-mentor-skill/references/**/*.md`
- `gh api repos/alchaincyf/x-mentor-skill`

## How Many Times We Will Test

- Static checks: once per claim, all in this single eval run.
- Live runs: 0 — deferred.

## What Counts As Passing

- claim-001 pass: `yaml.safe_load` returns a dict with non-empty `name` and `description`.
- claim-002 pass: every referenced markdown file exists and line counts roughly match SKILL.md's index.
- claim-003 pass: 5/5 README capabilities have a dedicated scenario block in SKILL.md (cite line range).
- claim-005 pass: ≥ 4 explicit, quotable style/format rules in SKILL.md.
- claim-006 pass: spot-checked research file shows direct primary-source URLs marked `(一手)`.
- claim-007 pass: license MIT, pushed within 90 days, author has verifiable public identity.

## If Everything Passes, What We Can Trust

- Minimum: `available` — structure verified, methodology source-traceable, repo trustworthy.
- Why not higher: claim-004 (live LLM run) skipped — README's self-reported A/B table is not third-party reproduction.
- Remaining risk: real LLM behavior under scenarios A–E unverified; Scenario E depends on host runtime computer-use; algorithm weight snapshot dated 2026-04 (already 1+ month old at eval time).
