# CLIProxyAPI eval plan — 2026-05-04

> Archetype: **api-service**. Layer: **molecule** (config-driven
> routing across predefined translators; no runtime LLM-driven
> decisions).

## What this repo claims

- Wraps Gemini CLI / Antigravity / ChatGPT Codex / Claude Code as
  OpenAI / Gemini / Claude / Codex compatible HTTP APIs
- 7 protocol translators, 7 OAuth login flows
- Multi-account round-robin load balancing per provider
- Streaming, function calling, multimodal
- Reusable Go SDK with EN + CN docs
- Cross-platform binaries (darwin / freebsd / linux / windows ×
  aarch64 + amd64)

## Validate now (static)

| Claim | Method |
|---|---|
| 001 7 translators | gh api internal/translator/ |
| 002 8 release binaries | gh api releases (latest) |
| 003 7 OAuth flows | gh api internal/auth/ |
| 004 multi-account config | grep config.example.yaml |
| 005 SDK + docs | curl docs/sdk-usage.md + gh api sdk/ + docs/ |
| 007 sponsor disclosure | inspect README structure |

## Cannot validate now (claim-006)

Live OpenAI-compatible API call requires (a) running binary, (b) a
real OAuth session, (c) curl test, (d) inspect response. Wendy
already runs CLIProxyAPI locally per memory record but this eval is
independent static-only.

## What counts as passing

- 001..005, 007: documented surface real
- 006 (deferred): one logged successful round-trip on
  `/v1/chat/completions`

## If everything passes

- Static-only: `usable`
- + 1 live call: still `usable` (single-evaluator)
- + multi-evaluator + multi-translator coverage: `reusable`
  candidate. Path to `recommendable` is plausible because the project
  is mature and well-released, but needs documented stability data.

## Evidence collected

`runs/2026-05-04/run-static-checks/`:

- `artifacts/{go.mod, sdk-usage.md, config.example.yaml}`
- `logs/{translators.txt, release-assets.txt, auth-providers.txt,
  internal-dir.txt, sdk-dir.txt, docs-dir.txt, static-evidence.txt}`
- `run-summary.yaml`, `business-notes.md`
