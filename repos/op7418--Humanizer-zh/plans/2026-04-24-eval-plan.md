# Eval Plan — op7418/Humanizer-zh (prompt-skill)

Pure prompt-skill: SKILL.md (484 lines) + README.md, no code, no sub-files.
Triggered in Claude Code as `/humanizer-zh <text>`. Target cloned from
github.com/op7418/Humanizer-zh @ 91f3d39 on 2026-04-24.

## What This Repo Claims

- **claim-001** — SKILL.md frontmatter loads cleanly (name, description,
  allowed-tools, metadata.trigger)
- **claim-002** — No broken internal references (fully self-contained)
- **claim-003** — 24 AI-writing tells promised in README are all covered in
  SKILL.md (### 1 through ### 24 with before/after examples)
- **claim-004** — A live run on an AI-slop Chinese paragraph produces a
  humanized output with zero blacklisted tokens
- **claim-005** — The 5-dim × 10-point rubric (直接性 / 节奏 / 信任度 /
  真实性 / 精炼度) is concrete enough to score output consistently
- **claim-006** — At least one install path works end-to-end
- **claim-007** — Content is adapted for Chinese, not machine-translated

## What We Will Validate

### Static layer (supporting evidence)

- **claim-001** — `python3 -c "import yaml; yaml.safe_load(open('SKILL.md'))"`
  on the frontmatter block, assert required keys present
- **claim-002** — `grep -E '\./(references|templates|commands)' SKILL.md` →
  expect 0 matches; also confirm file tree is just SKILL.md + README.md + LICENSE
- **claim-003** — cross-map README 24 patterns (6+6+6+6) to SKILL.md
  `### 1` through `### 24` headers
- **claim-006** — confirm repo contents fit Claude Code skill shape so
  `git clone <url> ~/.claude/skills/humanizer-zh` works even if the
  `npx skills add` path doesn't
- **claim-007** — grep for Chinese-specific adaptations (「」 note, 织锦
  in vocab list, Chinese place-name examples)

### Live layer (core — required for > 'usable')

- **claim-004** — feed a deliberately AI-sloppy Chinese paragraph (13
  marked tells: 否定式排比 / 三段式 / AI 词汇 / 模糊归因 / emoji 标题 /
  谄媚语) through the skill instructions in a Claude Code session,
  capture input + output as artifacts
- **claim-005** — apply the 50-point rubric to the output, record
  per-dimension score

## Real Inputs We Will Use

- Input A: OpenAI puff-piece in Chinese, ~280 chars, hand-seeded with
  13 of the 24 patterns (see `artifacts/01-input.md`)

## How Many Times We Will Test

- Static checks: once (structure is frozen per commit)
- Live run: 1 session, recorded as `runs/2026-04-24/run-live-humanize-1/`

## What Counts As Passing

- Static: zero broken refs, every README pattern mapped to a SKILL heading
- Live (claim-004): the humanized body region (excluding the
  change-log table) contains zero hits against a 17-token blacklist
  (此外 / 凸显 / 彰显 / 深深植根 / 不断演变 / 关键作用 / 令人叹为观止 /
  充满活力 / 好问题 / 希望这对您 / 作为…的证明 / 不仅仅是 / 坐落在 /
  丰富的…底蕴 / 无缝 / 直观 / 开创性)
- Live (claim-005): rubric score recorded, falls in one of the three
  bands defined by SKILL.md

## If Everything Passes, What We Can Trust

- Minimum trust: `usable` — structure is real but core untested
- Achieved trust: `reusable` — core run passed, portable evidence
- Would take us to `recommendable`: ≥ 2 independent live runs across
  different genres (marketing / academic / blog) with consistent
  rubric scores, and a verified `npx skills add` install path
