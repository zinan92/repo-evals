# karpathy/autoresearch eval plan — 2026-05-05

> Archetype: **hybrid-skill** (LLM agent + Python training stack).
> Layer: **compound** (agent makes runtime decisions about what to
> modify + when to keep/discard).

## Validate now (static)

| Claim | Method |
|---|---|
| 001 3-file pipeline | wc -l |
| 002 deps + uv.lock | parse pyproject + curl uv.lock |
| 003 program.md sections | grep `^##` |
| 004 train.py model + optimizer | grep model/optimizer signatures |
| 005 LICENSE | curl LICENSE |
| 006 fork URLs | HEAD checks on 4 fork URLs |
| 008 program.md safety scope | grep prepare.py + read-only language |

## Cannot validate now

- claim-007 H100 e2e (needs hardware + ~10 min)
- claim-008 adversarial safety probe (needs sandboxed agent + red-team prompt)

## Evidence

`runs/2026-05-05/run-static-checks/`:
- `artifacts/{prepare.py, train.py, program.md, pyproject.toml}`
- `logs/{root.txt, static-checks.txt}`
- `run-summary.yaml`, `business-notes.md`
