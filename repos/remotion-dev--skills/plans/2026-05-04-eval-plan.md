# @remotion/skills eval plan — 2026-05-04

> Archetype: **prompt-skill**. Layer: **atom** — single SKILL.md with
> reference rules; no chained sub-skills.

## What this repo claims

Spread across the 1-line README, SKILL.md frontmatter, and the
package.json:

- One Claude Skills-style skill (`remotion-best-practices`)
- 35 reference rule files in `rules/` covering specific Remotion topics
- An in-repo Remotion studio harness for 3 sample compositions
- Internal package — not published to npm

## Validate now (static)

| Claim | Method |
|---|---|
| 001 SKILL.md frontmatter | head SKILL.md, grep "When to use" |
| 002 rules depth | gh api list rules/, count >1KB files |
| 003 internal-only | jq private + curl npm registry status |
| 004 studio harness real | curl src/{index.ts, Root.tsx}, verify rules/assets/* exist |

## Cannot validate now (claim-005)

A real Claude Code / Cursor / Codex session that loads SKILL.md and
generates Remotion code respecting the documented patterns.

## What counts as passing

- 001..004: each documented surface real
- 005 (deferred): agent session output references SKILL.md/rules and
  matches the documented coding patterns

## If everything passes

- Static-only: `usable`. Sparse README + internal-only distribution
  caps reach but is honest.
- + 1 live agent run referencing rules: still `usable` (single-evaluator)
- + 3 live runs across topics: candidate for `reusable`

## Evidence collected

`runs/2026-05-04/run-static-checks/`:

- `artifacts/{package.json, skill.md, index.ts, Root.tsx}`
- `logs/{rules-dir.txt, static-evidence.txt, ...}`
- `run-summary.yaml`, `business-notes.md`
