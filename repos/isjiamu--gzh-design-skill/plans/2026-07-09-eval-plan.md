# Eval Plan — gzh-design-skill (hybrid-skill / molecule)

Claim-first plan. Support layer = deterministic scripts (fully testable
statically). Core layer = LLM-driven assembly (molecule — needs a live e2e to
lift `core_layer_tested`). Evidence in `runs/2026-07-09/run-static-and-live-e2e/`.

> **Ceiling note.** A hybrid-skill whose core layer is untested caps at `usable`.
> We ran one live molecule e2e (claim-010) to lift the core layer, and set
> `evidence_completeness: portable` (reproducible but not exhaustive) → caps at
> `reusable`. To reach `full`/recommendable, exercise claim-012/013/014.

## Claims under test

### Support claims (deterministic)
- **claim-001** — validate_gzh_html.py deterministically flags platform-forbidden HTML.
- **claim-002** — validate_gzh_html.py enforces the `<span leaf>` paste-safety wrap.
- **claim-003** — clean HTML passes (exit 0); half-width punctuation is WARNING not ERROR.
- **claim-004** — component_lint.py scans all component libraries at 0 ERROR.
- **claim-005** — shipped gallery output (docs/gallery/moyu-green.html) is actually compliant.
- **claim-006** — extract_docx.py uses only the Python stdlib (zero third-party deps).
- **claim-007** — wrap_preview.py emits a one-click "复制到公众号" preview page.
- **claim-008** — SKILL.md references all resolve to real files.
- **claim-009** — repo carries a genuine full AGPL-3.0 LICENSE.

### Core claims (LLM-driven user value)
- **claim-010** — Markdown → themed, paste-safe WeChat HTML that passes validation (the molecule promise).
- **claim-011** — fires on WeChat-formatting intent, not on web/PPT/write-an-article asks.
- **claim-012** — theme-generator second workflow produces a new registered theme (UNTESTED).
- **claim-013** — docx/pdf/txt normalize into faithful Markdown (UNTESTED).
- **claim-014** — consistent output across models / 换模型不走样 (UNTESTED).

## How each was validated

| Claim | Method | Result |
|---|---|---|
| claim-001 | Feed non-compliant HTML via `--stdin` | 4 ERROR, exit 1 |
| claim-002 | Non-wrapped vs wrapped HTML; count leaf in shipped gallery | enforced; gallery 122 wraps |
| claim-003 | Compliant HTML + half-width sample | exit 0; half-width → WARNING |
| claim-004 | `component_lint.py .` on the repo | 11 libs, 0 ERROR, exit 0 |
| claim-005 | `validate_gzh_html.py docs/gallery/moyu-green.html` | 完全合规, exit 0 |
| claim-006 | AST scan of imports | stdlib only |
| claim-007 | Run `wrap_preview.py`, grep output | 复制到公众号 + execCommand |
| claim-008 | Diff SKILL.md references vs file tree | all resolve |
| claim-009 | Read LICENSE head + `gh api .../license` | full AGPL-3.0 (NOASSERTION due to preamble) |
| claim-010 | **LIVE E2E**: assemble moyu-green section from sample-article.md; validate self-output | 0 ERROR / 0 WARN / 13 leaf, exit 0 |
| claim-011 | Static review of SKILL.md description + eval-cases.md pos/neg sets | passed_with_concerns (no live trigger_test) |
| claim-012/013/014 | Deferred | untested |

## Constraints (Mark governance rules)
- Isolated depth-1 clone at `/tmp/gzh-eval`; no install into live agent config.
- No credentials / browser session / network writes.
- Scripts run in isolated subprocess; primary-source evidence only.
