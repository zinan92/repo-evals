# QuantDinger eval plan — 2026-05-04

> Archetype: **orchestrator**. Layer: **compound** (multi-agent AI
> research, LLM-driven strategy/indicator gen, ensemble + reflection).

## What this repo claims

From `README.md` + `docker-compose.yml` + `mcp_server/pyproject.toml`
+ `backend_api_python/{Dockerfile, requirements.txt, env.example}`:

- 4-service Docker Compose stack (postgres + redis + Flask + React)
- Python 3.10+ source / 3.12 in Docker image
- Multi-LLM (OpenAI / DeepSeek / Grok) with custom BASE_URL
- Multi-broker (crypto via ccxt, stocks via ib_insync, forex via MT5)
- MCP server for Cursor / Claude Code / Codex / OpenClaw / NanoBot
- Localhost-only port bindings by default for sensitive services
- Live trading is opt-in (`AGENT_LIVE_TRADING_ENABLED=false`)

## Validate now (static)

| Claim | Method |
|---|---|
| 001 4-service compose | parse docker-compose.yml |
| 002 base-image consistency | grep Dockerfile + compose ARG |
| 003 multi-LLM | grep env.example for *_API_KEY/*_BASE_URL |
| 004 multi-broker | grep requirements.txt |
| 005 MCP server | parse mcp_server/pyproject.toml |
| 006 default port bindings | jq services.*.ports |
| 008 live-trading default off | grep env.example |

## Cannot validate now

- **claim-007 MCP-driven e2e** — install + LLM key + agent session
- **claim-008 live-order gating in practice** — needs paper-broker
  account + flip the flag in test, observe whether manual approval
  is enforced

## Real inputs

- raw URLs for compose, Dockerfile, requirements.txt, env.example,
  mcp_server/pyproject.toml
- gh api for backend_api_python and mcp_server contents

## What counts as passing

- 001..006 + 008: each claim's structure visible in the file
  responsible for it
- 007 (deferred): agent session produces a real backtest artefact
- 008 (live confirmation): manual approval step blocks LLM auto-fire

## If everything passes

- Static-only: `usable` (compound rule cap)
- + 1 logged compound run with happy path: `usable` confirmed,
  unblocks path to `reusable`
- + 3 compound scenarios (happy / failure / long-horizon): candidate
  for `reusable`
- `recommendable` requires multi-evaluator coverage and clear story
  for the live-order safety claim

## Evidence collected this run

`runs/2026-05-04/run-static-checks/`:

- `artifacts/{docker-compose.yml, env.example, mcp-pyproject.toml,
  backend-Dockerfile, backend-requirements.txt}`
- `logs/{compose-services.txt, backend-dirs.txt, backend-files.txt,
  mcp-server.txt, static-evidence.txt}`
- `run-summary.yaml`, `business-notes.md`
