# tab-out eval plan — 2026-05-04

> Archetype: **adapter** (Chrome tabs API wrapper). Layer: **atom** —
> single-purpose extension, no internal multi-skill composition.

## What this repo claims

From `README.md` + `extension/manifest.json`:

- Replaces Chrome new-tab page with a domain-grouped dashboard of
  open tabs
- Manifest V3, minimal permissions
- No external API calls, "100% local"
- Saved tabs in `chrome.storage.local`
- Sound synthesized via Web Audio (no audio files)
- Confetti via JS particles (no asset files)

## Validate now (static)

| Claim | Method |
|---|---|
| 001 MV3 + newtab override | jq manifest.json |
| 002 minimal permissions | jq permissions + host_permissions |
| 003 no external API calls | grep app.js / background.js for fetch / XHR / https?:// |
| 004 chrome.storage.local only | grep storage APIs |
| 005 synthesized sound, no audio files | grep AudioContext + ls extension/icons |

## Cannot validate now (claim-006)

Live install in a real Chrome — a human + browser are needed to verify
that the rendered dashboard matches the README description.

## Real inputs

- raw URLs: extension/{manifest.json, app.js, background.js}
- gh api: repos/zarazhangrui/tab-out/contents/extension

## What counts as passing

- 001..005: each documented mechanism present in the manifest /
  source. claim-003 is `passed_with_concerns` if the static check
  finds external requests that contradict the README headline.
- 006 (deferred): UX matches README description in real Chrome.

## If everything passes

- Static-only: `usable` — extension is real, minimal, MV3-conformant.
- + 1 logged live run: still `usable` (single-evaluator).
- Multi-evaluator + multi-Chrome-version: candidate for `reusable`.

## Evidence collected

`runs/2026-05-04/run-static-checks/`:

- `artifacts/{manifest.json, app.js, background.js}`
- `logs/extension-tree.txt`, `logs/static-evidence.txt`,
  `logs/favicon-leak.txt`
- `run-summary.yaml`, `business-notes.md`
