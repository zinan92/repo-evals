# xiaohongshu-skills eval plan — 2026-05-04

> Archetype: **hybrid-skill** (LLM + Python bridge + Chrome
> extension). Layer: **molecule** (5 atomic sub-skills wired by a
> root routing layer).

## What this repo claims

- 5 named sub-skills (auth / publish / explore / interact / content-ops)
- Root SKILL.md routes by user intent
- Chrome MV3 extension drives the user's real logged-in XHS session
- Python 3.11+, `uv sync` install
- Compatible with OpenClaw and Claude Code

## Validate now (static)

| Claim | Method |
|---|---|
| 001 5 sub-skills | HEAD checks on each SKILL.md |
| 002 root SKILL.md routing | grep sub-skill names in root SKILL.md |
| 003 extension permissions | jq manifest.json |
| 004 Python deps | parse pyproject.toml |
| 005 OpenClaw / Claude Code contract | parse SKILL.md frontmatter |

## Cannot validate now (claim-006)

Composite workflow on a real XHS account — needs a logged-in Chrome
profile + the user's XHS account (and willingness to accept rate-
limiting risk).

## What counts as passing

- 001..005: each documented surface real; concerns disclosed when the
  README understates a privileged operation
- 006 (deferred): composite request produces correct sub-skill
  ordering + side effects on a real account

## If everything passes

- Static-only: `usable`
- + 1 logged composite run: still `usable` (compound flow,
  single-evaluator)
- + 3 composite runs across different intents (publish / search /
  interact / heat-track): candidate for `reusable`

## Evidence collected

`runs/2026-05-04/run-static-checks/`:

- `artifacts/{pyproject.toml, SKILL.md, manifest.json}`
- `logs/{skills-dirs.txt, static-evidence.txt}`
- `run-summary.yaml`, `business-notes.md`
