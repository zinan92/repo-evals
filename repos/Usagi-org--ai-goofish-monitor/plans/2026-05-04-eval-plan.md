# ai-goofish-monitor eval plan — 2026-05-04

> Archetype reclassified: **api-service** (FastAPI + Vue web UI +
> scheduler). Layer: **molecule** (predefined LLM-criteria → scrape →
> LLM-analyze → notify pipeline; no runtime LLM-driven routing).

## What this repo claims

- One-line `docker compose up`, Web UI on :8000
- Web-UI managed multi-task scraping of XianYu (闲鱼) via Playwright
- Multi-modal LLM filtering with natural-language criteria
- 6 notification channels: ntfy / Bark / 企业微信 / Telegram /
  Gotify / Webhook
- Companion Chrome extension to export logged-in cookie
- SQLite primary store, default at `data/app.sqlite3`
- Multi-account + proxy-pool rotation

## Validate now (static)

| Claim | Method |
|---|---|
| 001 docker-compose minimal | parse compose YAML |
| 002 DDD layering | gh api src/ |
| 003 6 notification channels | grep .env.example section headers |
| 004 multi-stage Dockerfile | grep `^FROM ` |
| 005 chrome extension MV3 | jq manifest.json |
| 006 prompt templates substantive | wc -l prompts/*.txt |
| 007 admin password DX | grep .env.example for warnings |

## Cannot validate now (claim-008)

Live monitoring run with real XianYu cookie, real LLM key, real ntfy
endpoint.

## What counts as passing

- 001..006: documented surface real
- 007: passed_with_concerns if defaults are noted but not strongly
  warned
- 008 (deferred): real end-to-end notification on a real listing

## If everything passes

- Static-only: `usable`
- + 1 live run: still `usable` (single-evaluator)
- + multi-evaluator + edge-case scenarios (expired cookie, IP block,
  LLM down): `reusable` candidate

## Evidence collected

`runs/2026-05-04/run-static-checks/`:

- `artifacts/{docker-compose.yaml, Dockerfile, env.example,
  requirements.txt, extension-manifest.json}`
- `logs/{src-dirs.txt, web-ui-dirs.txt, prompts.txt,
  static-evidence.txt}`
- `run-summary.yaml`, `business-notes.md`
