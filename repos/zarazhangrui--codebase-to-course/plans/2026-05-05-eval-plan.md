# codebase-to-course eval plan — 2026-05-05

> Archetype: **hybrid-skill** (LLM + real CSS/JS/build.sh assets).
> Layer: **molecule** (SKILL.md drives a multi-stage pipeline).

## What this repo claims

- A Claude Code skill that turns any codebase into an interactive
  HTML course (single-page, no dependencies, works offline)
- Output: scroll-based modules + animated visualizations + embedded
  quizzes + code↔plain-English side-by-side + glossary tooltips
- Self-contained — copy `codebase-to-course/` into `~/.claude/skills/`,
  trigger via 7 documented phrases

## Validate now (static)

| Claim | Method |
|---|---|
| 001 SKILL.md frontmatter + triggers | head SKILL.md, grep quoted phrases |
| 002 real CSS/JS/build.sh assets | wc -l on each |
| 003 reference docs ≥ 2KB each | wc -c on 5 docs |
| 004 build.sh is real assembler | cat build.sh |
| 005 directory matches README | gh api root + references/ |
| 006 LICENSE present | curl LICENSE |

## Cannot validate now (claim-007)

Live agent run on a real codebase — needs Claude Code session.

## What counts as passing

- 001..005: documented surface real
- 006: LICENSE present at root
- 007 (deferred): generated course actually opens + reads usefully

## If everything passes

- Static-only with LICENSE present: ~70 (Self-use)
- Without LICENSE: ~63 (Try) — penalty for legal ambiguity
- With 1 logged live run: still ~65-70 (single evaluator)
- Multi-evaluator + multiple test codebases: candidate for 75+

## Evidence collected

`runs/2026-05-05/run-static-checks/`:

- `artifacts/{SKILL.md, ref-build.sh, ref-styles.css, ref-main.js,
  ref-_base.html, ref-_footer.html}`
- `logs/{references.txt, static-checks.txt, static-evidence.txt}`
- `run-summary.yaml`, `business-notes.md`
