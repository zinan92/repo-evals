# Eval Plan — guizang-material-illustration (prompt-skill / molecule)

Claim-first plan. Support layer = static, verifiable facts about the prompt
material (fully checkable statically). Core layer = the real user outcome is an
IMAGE rendered by an EXTERNAL image model — this governance eval had NO
image-model credentials, so the core layer is UNTESTED and the verdict caps at
`usable` (category `available`).

> **Ceiling note.** A prompt-skill whose core LLM/image-model-driven layer is
> untested caps at `usable`. We did NOT run a live image generation (no
> credentials, and generating would touch external model quotas outside the
> isolated clone). `core_layer_tested: false`, `evidence_completeness: partial`.

## Claims under test

### Support claims (static, verifiable)
- **claim-001** (critical) — SKILL.md's 6 references + assets/prompt-template.md all resolve; frontmatter valid.
- **claim-002** (high) — the 6 reference files are internally coherent, non-contradictory.
- **claim-003** (high) — complete constraint-rich prompt scaffolds (base shell + 5 structures + variants).
- **claim-004** (high) — chart-beautify's data-first "never fake the numbers" protocol + Required-accuracy block.
- **claim-005** (high) — trigger metadata is specific (description + openai.yaml) — design only, not live-fired.
- **claim-006** (medium) — qa-checklist covers the real failure modes before delivery.
- **claim-007** (medium) — the 配图层×排版层 boundary with social-card / PPT skills is explicit.
- **claim-008** (medium, FAILED) — NO LICENSE: default all-rights-reserved (adoption flag).

### Core claims (image-model-dependent — UNTESTED, no credentials)
- **claim-009** (critical) — external image model renders a usable, on-brand labeled illustration (UNTESTED).
- **claim-010** (high) — chart output preserves the exact numbers in the rendered image (UNTESTED).
- **claim-011** (high) — in-image Chinese labels render legibly, not garbled (UNTESTED).
- **claim-012** (medium) — reference-informed images are factually accurate (UNTESTED).
- **claim-013** (medium) — output stays legible at social-card size + hands off cleanly (UNTESTED).

## How each was validated

| Claim | Method | Result |
|---|---|---|
| claim-001 | Diff SKILL.md references vs file tree; parse frontmatter | all 7 resolve; frontmatter valid → passed |
| claim-002 | Read all 6 references; cross-check rules | consistent (labels/accent/data/logo rules) → passed |
| claim-003 | Inspect prompt-template.md + prompt-patterns.md | base shell + 6 structure shells + label-repair → passed |
| claim-004 | Inspect chart-beautify.md | keep/discard + Required-accuracy + reject-if-wrong → passed |
| claim-005 | Read frontmatter description + agents/openai.yaml | rich triggers + allow_implicit_invocation → passed_with_concerns (no live trigger_test) |
| claim-006 | Inspect qa-checklist.md | 5 groups covering labels/data/crop/reference/edu → passed |
| claim-007 | Read SKILL.md + use-cases-and-routing.md Boundary | explicit deferral to social-card/PPT → passed |
| claim-008 | `gh api .../license` + contents/LICENSE(.md) | license=null; both 404 → FAILED (no license) |
| claim-009–013 | Deferred — no image-model credentials | UNTESTED (image-model-dependent) |

## Real inputs we will use (deferred to a credentialed run)
- Mechanism/process concept (e.g. an agent loop) → labeled cycle/pipeline diagram.
- Ugly benchmark bar-chart screenshot → data-first material redraw (verify exact values).
- Niche concept (PKCE / Zettelkasten / lever) → reference-informed diagram.

## What counts as passing (for the deferred core run)
- Core: rendered images score well on qa-checklist (in-image Chinese label accuracy, data
  fidelity, no crop, on-brand), across ≥3 topics → lifts core_layer_tested + evidence→portable.

## Constraints (Mark governance rules)
- Isolated depth-1 clone at `/tmp/veval-guizang-material`; no install into live agent config.
- No credentials / browser session / image-model calls; primary-source evidence only.
- Because the core deliverable is image-model-dependent, static eval alone caps at `available`.
