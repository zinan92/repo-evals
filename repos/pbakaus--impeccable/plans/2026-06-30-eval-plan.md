# Eval Plan — impeccable (hybrid-skill)

> **Ceiling reminder.** A hybrid-skill whose core LLM layer is untested caps
> the overall verdict at `usable`. Impeccable's deterministic support layer is
> strong and verified live; the core "makes AI better at design" layer was NOT
> run live, so the ceiling applies by design.

## What This Repo Claims

### Support claims (deterministic) — VERIFIED
- claim-001: Deterministic detector runs with no API key.
- claim-002: 44 deterministic detector rules actually exist.
- claim-003: 23 commands ship in the skill.
- claim-004: SKILL.md references all resolve.
- claim-005: Installs across 14 agent harnesses.

### Core claims (LLM-driven user value) — UNTESTED (ceiling cap)
- claim-101: Actually makes AI output better at design (quality contract).
- claim-102: Anti-slop rules followed in real generated output.

## What We Validated

### Support layer (done)
- claim-001: ran `node cli/bin/cli.js detect slop.html` in isolated clone, no key → 5 anti-patterns reported.
- claim-002: grep of `cli/engine` → 48 unique rule ids (≥ claimed 44).
- claim-003: counted reference/*.md command docs (28 docs, 23 commands).
- claim-004: SKILL.md relative refs all resolve.
- claim-005: 14 harness payload dirs present + skill installed at ~/.claude/skills/impeccable.

### Core layer (deferred)
- claim-101 / claim-102: require ≥2 real agent sessions generating UI, graded against
  SKILL.md quality contract + detector re-check. Not performed this round.

## Real Inputs Used
- Input A: hand-written `slop.html` (Inter, purple gradient, nested cards, bounce easing).
- Input B: repo source tree (claim/command/rule counts).
- Input C: `gh api` metadata (license, stars, releases, activity).

## What Counts As Passing
- Support: zero failed deterministic checks → PASS.
- Core: artifact ≥ N/M on quality contract, anti-slop clean, repeatable → NOT RUN.

## Trust Level
- Achieved: `usable` — support strong + verified, core unverified.
- Stretch (next round): one live `/impeccable craft`+`critique` session lifts toward `reusable`/production.
- Remaining risk: agent-runtime drift, prompt-model compatibility, subjective quality lift unproven.
