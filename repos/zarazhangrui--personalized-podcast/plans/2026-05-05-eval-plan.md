# personalized-podcast eval plan — 2026-05-05

> Archetype: **hybrid-skill**. Layer: **molecule** (predefined
> content → script → TTS → stitch → MP3 pipeline).

## Validate now (static)

| Claim | Method |
|---|---|
| 001 SKILL.md + PROMPT.md | wc -l on each |
| 002 4 Python scripts | wc -l on each |
| 003 deps coverage | grep requirements.txt |
| 004 RSS template | grep `<rss / <channel / <item / {{ }}` |
| 005 config coverage | grep config.example.yaml |
| 006 LICENSE | curl |
| 007 install path correctness | grep README + verify gh api |

## Cannot validate now (claim-008)

Live `/podcast` flow needs Claude Code + Fish Audio key.

## Evidence

`runs/2026-05-05/run-static-checks/`:
- `artifacts/{SKILL.md, PROMPT.md, scripts-*, config-example.yaml, feed-template.xml}`
- `logs/{root.txt, static-checks.txt, extra-checks.txt}`
- `run-summary.yaml`, `business-notes.md`
