# Eval plan — op7418/guizang-social-card-skill (2026-07-09)

Governance eval for Park Agent OS (Mark) → feeds the Vera 视觉/配图 lane. Claim-first plan; claim IDs reference `claims/claim-map.yaml`.

## Approach
- **Layer**: molecule (fixed pipeline: intake → page-plan → style/theme → copy seed → fill 28 recipes → HTML→PNG → validate). hybrid-skill archetype.
- **SUPPORT layer** (deterministic, testable statically): claim-001 through claim-007 — doc validator, design-system substance, SKILL-ref integrity, templates, license.
- **CORE layer** (LLM molecule + HTML→PNG render): claim-008 (determinism-by-construction), claim-009 (on-brand PNG), claim-010 (R1-R9 layout gate), claim-011 (trigger/scope), claim-012 (scope discipline), claim-013 (Live Photo — deferred), claim-014 (web image sourcing — deferred).

## Run 1 — run-static-and-render-attempt (isolated clone /tmp/veval-guizang-card @ cf4b810)
1. `gh api` facts (4808★, spdx AGPL-3.0, 0 releases/tags) — feeds repo.yaml maintainer/ecosystem signals.
2. `node scripts/check-skill-docs.mjs` → 27/27 PASS, exit 0 — **claim-001** (doc-validator-ran), **claim-003** (SKILL-refs-resolve).
3. Static grep: M01-M16 + S01-S12 = 28 recipes; 10 themes; template data-theme/data-accent wiring — **claim-002** (28×10-design-system-wired), **claim-004** (templates-self-contained).
4. Read `validate-social-deck.mjs` full source (547 lines, real R1-R9 DOM validator) — **claim-005** (real-9-rule-validator-source).
5. Read LICENSE + COMMERCIAL_LICENSING.md — **claim-006** (AGPL-real).
6. Read Live-Photo pipeline docs — **claim-007** (Live-Photo-pipeline-documented).
7. Static review of determinism-by-construction (fixed-px CSS, no image model/RNG) — **claim-008** passed.
8. `npm install` (playwright 1.60, exit 0); `npx playwright install chromium` → BLOCKED (full build download throttled >3min).
9. Assembled a real 公众号 21:9+1:1 封面对 from the Swiss seed; `node validate-social-deck.mjs` → BLOCKED (chromium_headless_shell-1223 missing) — **claim-009**, **claim-010** left `untested` with skip_reason.
10. SKILL.md description + scope review — **claim-011** (trigger, passed_with_concerns — no live trigger_test), **claim-012** (scope discipline, passed).

**Outcome of run 1**: core_layer_tested=false, evidence_completeness=partial → verdict capped at usable (🛠 Available). Score 77/100.

## Run 2 — run-static-and-live-render (same day follow-up, closes the run-1 gap)
1. `npx playwright install chromium` retried → succeeded this time (~260MB, chromium_headless_shell-1223 installed).
2. `node validate-social-deck.mjs` on the run-1 sample deck → 1st pass: 1 clean/0 fail/1 WARN (R6: 21:9 title wrapped 2 lines, over the 1-line cap) — a real defect the validator caught.
3. Fixed the title ("Skill 是能力商品，不是提示词" → "Skill 是能力商品") → 2nd pass: 2/2 clean, 0 fail, 0 warn, exit 0 — **claim-010** flips to `passed`.
4. Screenshotted both poster sections to real PNG (isolated per-board viewport) → visually inspected: clean Swiss layout, disciplined IKB accent use, legible Chinese, no defects found — **claim-009** flips to `passed`.

**Outcome of run 2**: core_layer_tested=true, evidence_completeness=portable (single theme × single article, not full multi-theme/multi-article coverage) → verdict raised to `reusable` (🏭 Production display, capped by evidence-completeness ceiling short of `recommendable`). Score 85/100.

## Still deferred (not covered by either run)
- claim-013 (Live Photo → .pvt pipeline)
- claim-014 (web image sourcing)
- Multi-theme / multi-article batch consistency
- Real publish-to-WeChat-editor verification (only local PNG correctness was verified)

## Next step to reach full/recommendable
Multi-theme + multi-article batch render/validate, plus one real WeChat-editor publish test.
