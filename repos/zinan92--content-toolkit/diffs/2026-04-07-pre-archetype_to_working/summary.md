# Re-Eval Diff — zinan92--content-toolkit

**from:** `HEAD~2` (`7127d8c`) → **to:** `working`

**Comparison confidence:** medium
  - baseline provenance quality is 'missing' — changes attributed to 'HEAD~2' may be historical artifacts, not genuine eval movement
  - head snapshot at 'working' has no run-level provenance
  - baseline verdict bucket is 'unknown' — bucket delta is not meaningful

## Headline

Verdict did not change bucket (`unknown` → `unknown`).

## Verdict

- bucket: `unknown` → `unknown` (unchanged)
- archetype: `None` → `orchestrator` (changed)

## Claim movement

- improvements: **0**
- regressions: **0**
- newly failing (was untested): **0**
- claims added: 0
- claims removed: 0

## Runs

- baseline runs: 1
- head runs: 1

## Coverage gaps

- structured gap diff unavailable (baseline: missing, head: structured)
  - committed gap reports under `gap-reports/` are still human-readable, and a live run via `coverage_gap_detector.py` can produce the structured form.

## Snapshot integrity

- baseline provenance: `missing`
- head provenance: `missing`
