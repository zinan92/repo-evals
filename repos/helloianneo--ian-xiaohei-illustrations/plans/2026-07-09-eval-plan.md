# Eval Plan — ian-xiaohei-illustrations (prompt-skill)

Evaluated 2026-07-09 by Mark (Claude Code / claude-opus-4-8) for Park Agent OS,
feeding the Vera（视觉/配图 Manager）role, 正文配图 lane of park-content-v1.

> **Ceiling reminder.** The core LLM-driven layer here is an IMAGE produced by an
> EXTERNAL image model (Codex built-in image_gen / nano-banana / Gemini / Flux).
> This eval has no image-model credentials, so the core layer is UNTESTED and the
> verdict is capped at `usable` / 🛠 available. Scanning frontmatter + references is
> necessary but not sufficient — it validates the prompt scaffolding, never the image.

## What This Repo Claims

- Turns one cognitive anchor in a Chinese article into a 16:9 white-bg hand-drawn
  body illustration with a fixed "小黑" IP (README / SKILL.md 核心定位).
- Ships a reusable style system: style-DNA + 小黑 IP + 8 structure types + a
  parameterized per-image prompt template + a QA checklist (references/).
- Supports: shot-list planning, per-image generation, single-concept image,
  title-removal edit (README 怎么用 / examples/prompts.md).

## What We Will Validate

### Static layer (support) — DONE, reproducible

- claim-001 SKILL.md frontmatter parses + declares triggers — PASSED
- claim-002 all 5 references/ + assets/examples/ resolve — PASSED
- claim-003 README 4 capabilities ↔ SKILL/template coverage table — PASSED
- claim-004 style-DNA / IP / composition / QA are coherent + complete — PASSED
- claim-005 prompt-template is parameterized + model-agnostic — PASSED
- claim-006 trigger spec is specific (no live trigger_test run) — PASSED_WITH_CONCERNS
- claim-007 calibration example gallery present (8 + 14 PNG) — PASSED
- claim-008 real MIT license (gh api spdx_id=MIT) — PASSED

### Live layer (core) — NOT RUN (no image-model credentials)

- claim-009 generated image is on-brand + usable — UNTESTED
- claim-010 anti-slop (no PPT / cute / top-left title) respected — UNTESTED
- claim-011 fresh metaphor per article (non-clone) — UNTESTED
- claim-012 Chinese labels render legibly (no garbled text) — UNTESTED

## Real Inputs We Would Use (deferred to Vera)

- Input A: one real Chinese methodology long-read → 4-8 image shot list + renders
- Input B: one single-claim prompt (e.g. 信任是一块证据一块证据铺过去) → 1 render + edit

## How Many Times We Would Test

- Static checks: run once per commit (deterministic).
- Live runs: ≥ 2 independent agent sessions in Codex (or with an external image
  model wired to prompt-template.md) — deferred until credentials exist.

## What Counts As Passing

- Static: zero unresolved references, every README capability mapped — MET.
- Live: rendered images score green on qa-checklist.md (white bg, 40-60% subject,
  小黑 carries the action, legible sparse labels, no banned looks) across ≥2 runs.

## If Everything Passes, What We Can Trust

- Current trust level: `usable` / 🛠 available — prompt scaffolding is real +
  complete, core image output untested (image-model-dependent).
- Stretch trust level: `reusable` — ≥2 live renders pass qa-checklist → lifts
  core_layer_tested and evidence portable.
- Remaining risk: image-model dependency (Claude Code has no image_gen),
  cross-model / cross-article style drift, Chinese-label typos, bus-factor 1.
